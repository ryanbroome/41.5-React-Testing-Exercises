// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom

// TODO: Mentor, this had an error did I do this correclty to fix? I looked at the docs for Testing-Library/jest-dom, did a command F searching docs for some of the import syntax to find why it wasn't working, then updated with syntax they said replaced it during update 6.0.0
import "@testing-library/jest-dom/jest-globals";
